start project
    docker-compose up
   	docker-compose up --build
stop project
    docker-compose stop
    docker-compose down
kill project
    docker-compose kill
    docker-compose down --rm=all

mysql:
    login myapp
    pass myapp

docker bash:
    docker exec -it test_php /bin/bash

Problem private
    sudo chown -R $USER server
